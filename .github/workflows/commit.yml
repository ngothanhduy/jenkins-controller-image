name: Build and Test Jenkins Controller Image Workflow
on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v5
      - name: Checkout Repository
        run: git checkout ${{ github.ref }}
      - name: Build Jenkins Controller Image
        run: make build